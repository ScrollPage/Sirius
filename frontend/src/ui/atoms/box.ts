import styled from "@emotion/styled";

export const Box = styled.div`
  padding: 20px;
  background: #ffffff;
  box-shadow: 0px 0px 40px rgba(0, 0, 0, 0.08);
  border-radius: 20px;
  position: relative;
`;